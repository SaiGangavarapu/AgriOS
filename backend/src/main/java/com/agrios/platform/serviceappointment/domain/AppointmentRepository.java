package com.agrios.platform.serviceappointment.domain; import java.time.Instant; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface AppointmentRepository extends JpaRepository<AppointmentEntity,UUID>{Optional<AppointmentEntity> findByIdAndTenantId(UUID id,UUID tenantId); List<AppointmentEntity> findByTenantIdAndFarmerIdOrderByScheduledStartAtDesc(UUID tenantId,UUID farmerId); List<AppointmentEntity> findByTenantIdAndProviderIdAndScheduledStartAtBetweenOrderByScheduledStartAt(UUID tenantId,UUID providerId,Instant from,Instant to); long countByTenantIdAndProviderIdAndStatus(UUID tenantId,UUID providerId,String status);}
