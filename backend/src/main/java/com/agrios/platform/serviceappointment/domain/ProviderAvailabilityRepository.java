package com.agrios.platform.serviceappointment.domain; import java.time.Instant; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface ProviderAvailabilityRepository extends JpaRepository<ProviderAvailabilityEntity,UUID>{Optional<ProviderAvailabilityEntity> findByIdAndTenantId(UUID id,UUID tenantId); List<ProviderAvailabilityEntity> findByTenantIdAndProviderIdAndStartAtBetweenAndStatusOrderByStartAt(UUID tenantId,UUID providerId,Instant from,Instant to,String status);}
