package com.agrios.platform.tasks.application;
import static org.junit.jupiter.api.Assertions.*; import static org.mockito.Mockito.*; import com.agrios.platform.cropcycle.domain.CropCycleRepository; import com.agrios.platform.tasks.domain.*; import java.time.Instant; import java.util.*; import org.junit.jupiter.api.Test;
class RecurringTaskServiceTest { @Test void generatesNoTasksWhenNothingIsDue(){var schedules=mock(RecurringTaskScheduleRepository.class);when(schedules.findByTenantIdAndActiveTrueAndNextRunAtLessThanEqualOrderByNextRunAt(any(),any())).thenReturn(List.of());var service=new RecurringTaskService(schedules,mock(TaskRepository.class),mock(TaskEventRepository.class),mock(CropCycleRepository.class));assertEquals(0,service.generateDue(UUID.randomUUID(),UUID.randomUUID(),Instant.now()));}}
