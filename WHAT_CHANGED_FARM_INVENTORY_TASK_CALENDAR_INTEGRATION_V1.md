# What Changed

- Added the `inventory` bounded context and V046.
- Added inventory items, lots, stock movements, low-stock calculation, and expiry lookup.
- Added recurring task schedules and explicit due-task generation.
- Added `/api/v1/work-management/calendar` and `/api/v1/work-management/farms/{farmId}/dashboard`.
- Extended task and operation repositories for calendar and dashboard queries.
- Added focused unit tests and implementation documentation.
