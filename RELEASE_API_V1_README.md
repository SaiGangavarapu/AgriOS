# AgriOS Documentation Baseline with API v1.0

This cumulative repository release preserves the uploaded AgriOS documentation
baseline and adds the complete initial API specification under
`docs/07-api-contracts`.

The local `.git` directory is intentionally excluded from the generated ZIP.
Apply the archive into the existing Git working tree, verify, commit, and push.
